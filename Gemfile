# frozen_string_literal: true
source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'compare_with_wikidata', github: 'everypolitician/compare_with_wikidata', branch: 'subpages'
gem 'puma'
gem 'sinatra'
